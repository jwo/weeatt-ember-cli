import {
  breaklines
} from 'cart/helpers/break-lines';

module('BreaklinesHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = breaklines(42);
  ok(result);
});
